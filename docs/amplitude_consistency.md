# Amplitude Consistency Strategy

To ensure seismic interpretability, amplitudes are restored using robust
percentile scaling followed by global mean correction.

This avoids domination by outliers while preserving relative energy.
